export type Car = {
          id? : number;
          brand : string;
          model : string;
          color : string;
          registrationNumber : string;
          modelYear : number;
          price : number;
}

export type User = {
          username: string;
          password: string;
}